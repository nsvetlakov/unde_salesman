import 'package:flutter/material.dart';

/// класс цветов приложения
class AppColors {
  ///Белый
  static const white = Color(0xFFFFFFFF);

  /// Черный
  static const black = Color(0xFF000000);

  /// Фон
  static const background = Color(0xFFFCFCFC);

  /// Текст
  static const textColor = Color(0xFF272727);

  /// Акцентный цвет - зеленый
  static const accent = Color(0xFF69C182);

  /// Дополнительный (разделители строк, контуры некоторых иконок)
  static const optLineSeparator = Color(0xFF9CAC9C);

  /// Дополнительный - светло-зеленый
  static const optlightGreen = Color(0xFFE9F0ED);

  /// Дополнительный - серый
  static const optGrey = Color(0xFF7C847F);

  /// Дополнительный (Splash некотЛрых элементов, которые находятся
  /// на фоне акцентного цвета 69С182)
  static const optForGreen = Color(0xFF9CD6AB);

  ///Дополнительный: кнопки товаров без подписки некоторые поля
  static const optProductBtn = Color(0xFFD6D6D6);

  /// Дополнительный (цвет цены акционных товаров цвет ошибки)
  static const red = Color(0xFFD33E3E);

  /// Дополнительный (плашка, которая обозначает акционные товары с надписью Promotion)
  static const promo = Color(0xFFD6432F);

  /// Дополнительный (плашка, которая обозначает товары TOP SALES)
  static const topSales = Color(0xFFFA8417);
}
